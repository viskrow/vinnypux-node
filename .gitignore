# ─── Секреты (сертификаты, ключи, токены) ────────────────────────────────
nginx/ssl/vinnypuxtomoon/*.pem
.env
*.env
.acme.sh/

# ─── Локальные конфиги и скрипты (содержат UUID/SECRET_KEY, не для git) ──
configs/
routings/
scripts/

# ─── Pearly-only (dev): NGENIX vinnyplace verification + compose override ─
# Не для прод-деплоя — иначе compose override ломает prod-ноды и палит vinnyplace.
# См. nginx.conf: include /etc/nginx/conf.d/local/*.conf
docker-compose.override.yml
nginx/conf.d/local/

# ─── Локальная документация и шпаргалки ─────────────────────────────────
docs/
CLAUDE.md
*.txt

# ─── Build artifacts ──────────────────────────────────────────────────────
landing/node_modules/
landing/dist/
node_modules/
dist/

# ─── Логи и runtime state ─────────────────────────────────────────────────
*.log
/var/

# ─── Backup-папки + архивы (НИКОГДА не должны попасть в git) ──────────────
# инцидент 2026-05-14: vinnypux-backup-*.tar.gz попал в публичный репо
*.bak
*.bak.*
*.backup
*.tar
*.tar.gz
*.tgz
*.tar.bz2
*.tar.xz
*.zip
*.7z
*.rar
# контрольные суммы архивов — сами архивы уже игнорим выше, хвост от них тоже не нужен
*.sha256
*.sha256sum
*-backup-*
*backup*
vbk/
vbk*/

# ─── Ключи / приватные креды (расширено) ──────────────────────────────────
*.key
*.pem
*.p12
*.pfx
/wg0.conf
*.crt
id_rsa
id_ed25519
id_ecdsa
*_rsa
*_ed25519
*_ecdsa
authorized_keys
known_hosts
.ssh/
ssh/

# ─── Старые / временные папки разработки ─────────────────────────────────
wl/
tmp/
.tmp/

# ─── IDE и редакторы ──────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~

# ─── Мониторинг-стек (перевезён с 144.31.147.29 2026-08-29) ───────────────
# Репо ПУБЛИЧНЫЙ. Внутри monitoring/ живут .env (Remnawave API JWT, TG bot-token,
# пароль Postgres, пароль Grafana), bot_token и приватные ключи LE ⇒ каталог
# целиком вне git, как configs/ и scripts/. Инцидент 2026-05-14 — ровно про это.
monitoring/

# ─── Claude Code ──────────────────────────────────────────────────────────
.claude/
memory/

# ─── OS ───────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
._*
